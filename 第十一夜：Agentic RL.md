**青年**：（推门进来，手里没拿电脑，反而拿着一张数学卷子）老师，我昨晚失眠了。

**哲人**：为什么？

**青年**：我在想，我们之前聊了那么多——智能体怎么想、怎么做、怎么记、怎么和外部通信。但有一个根本问题我一直没问：**它怎么“变强”？** 不是靠我们手写更多工具、更多提示词、更多框架，而是靠它**自己**变强。它有没有可能，像人一样，通过“练习”变得更好？

**哲人**：（接过那张卷子，看了一眼）你手里的，是一道数学题？

**青年**：是的。我在想，如果我把这道题给一个智能体，它一次答错了，它会不会“长记性”？它会不会在第二次、第三次的时候，自己找到更好的解法？还是说，它永远只能靠我给它更好的提示词？

**哲人**：（把卷子放在桌上，倒了两杯茶）你今晚带来的，是整个教程里最深的一个问题。前面十章，我们都在“构造”智能体——给它大脑、给它记忆、给它工具、给它通信。但这一章，我们不再“构造”它，我们**训练**它。

**青年**：训练？就像训练一个学生？

**哲人**：对。不是教它“这一题的答案是什么”，而是教它“怎么思考才能做对题”。不是告诉它“这一步该怎么做”，而是让它自己**试错**，从错误里学会更好的策略。

**青年**：这不就是强化学习吗？

**哲人**：对。而**Agentic RL**，就是把强化学习用在**智能体**身上的那一套方法。今晚我们要聊的，就是**一个智能体，怎么通过自己的经历，变得更强**。


## 一、从“模仿”到“试错”：两种学习的本质区别

**哲人**：我们先聊一个最根本的问题。你之前已经知道监督微调（SFT）了。它怎么训练模型？

**青年**：给模型看很多“问题-答案”对，让它模仿正确答案。

**哲人**：对。它的本质是**模仿**。你给它一个标准答案，它就学“下一次碰到类似的问题，我应该这么答”。这种方式有什么局限？

**青年**：它只能学到训练数据里有的东西。如果训练数据里都是“标准解法”，它就只能学“标准解法”。它没法发现更好的解法。

**哲人**：对。而且，它没有“探索”的空间。它只能沿着人类标注的路径走。它不知道“如果我不按标准解法走，会不会有更好的答案”。它不敢走弯路，因为它没见过“弯路”。

**青年**：所以，它永远只能达到人类标注的水平，不能超过。

**哲人**：这就是SFT的天花板。而强化学习打破了这堵墙。强化学习不告诉它“标准答案是什么”，它只告诉它“你的答案对不对”。然后让它**自己去试**。它可以在每一步生成**多个**候选答案，有的对，有的错。然后，对的那些得到奖励，错的那些得到惩罚。它从**自己的成功和失败**里，学会了“什么样的推理路径更可能通向正确答案”。

**青年**：所以它不是“模仿”，而是“探索”？

**哲人**：对。而且，它可能发现**人类都没发现过**的解题路径。就像AlphaGo下围棋，它自己琢磨出了很多人类棋手从没下过的招数，而且那些招数被证明是有效的。这就是强化学习的核心魅力：**它不局限于人类已有的知识，它通过试错，可以超越人类**。


## 二、Agentic RL的“状态”：它知道什么？

**哲人**：好，现在我们把强化学习放到智能体的语境里。先问你一个问题：一个智能体在解决一道数学题的时候，“它现在的状态”是什么？

**青年**：它看到的问题，和它已经写出来的推理步骤。

**哲人**：对。这比传统的强化学习复杂多了。传统的强化学习，比如训练一个打游戏的小人，它的状态是“屏幕上的画面”。但LLM智能体的状态是“文本上下文”——它看到的问题、它之前生成的每一步推理、它调用工具返回的结果。全都算它的状态。

**青年**：所以它的状态是**动态累积**的。它每走一步，状态就多一层。

**哲人**：对。而且它的行动也不只是“生成文字”。它可以选择“继续推理”，也可以选择“调用计算器”，还可以选择“结束回答”。它的行动空间比单纯的文本生成要丰富得多。

**青年**：那它的奖励呢？是每走一步都给，还是最后才给？

**哲人**：都有。有一种叫**稀疏奖励**，只在最后给。它答对了，+1；答错了，0。中间的过程不给反馈。还有一种叫**密集奖励**，每一步都给。比如“调用工具成功”+0.1，“推理步骤清晰”+0.1，“最终答案正确”+1。稀疏奖励信号少，训练难；密集奖励信号多，但设计起来要小心，不能让它“为了拿奖励而做没用的事”。

**青年**：那Agentic RL到底想让智能体学会什么？

**哲人**：六个能力。你记一下：**推理、工具使用、记忆、规划、自我改进、感知**。这六个，就是Agentic RL的“六边形战士”目标。

**青年**：一个一个说？

**哲人**：好。**推理**，就是从信息里逻辑地得出结论。**工具使用**，就是知道什么时候该用计算器、什么时候该用搜索引擎、什么时候该自己算。**记忆**，就是知道什么该记住、什么该忘。**规划**，就是知道为了最终目标，有时候要“绕路”先收集信息。**自我改进**，就是从错误里学习，不再犯同样的错。**感知**，就是能理解图像、声音等多模态信息。


## 三、训练的第一步：SFT——先学会“格式”，再学会“思考”

**哲人**：现在，我们来看训练流程。强化学习不是第一步。第一步是**SFT**。

**青年**：为什么不能直接强化学习？

**哲人**：因为强化学习需要一个“起点”。如果模型连基本的输出格式都不会——比如它不知道该输出“Step 1: ... Step 2: ... Final Answer: ...”，而是乱七八糟地写一堆——那它每次都是错的。它得不到有效的奖励信号，强化学习就没法进行。

**青年**：所以SFT是教它“基本规矩”。

**哲人**：对。SFT给它看很多“标准答案”，让它学会“解题应该长什么样”。比如GSM8K的题目，标准答案里会有中间步骤，最后用`#### 72`标记最终答案。SFT就是让它学会这种**格式和初步的推理模式**。

**青年**：但SFT之后，它只是“模仿”得很好，不一定真的“会思考”。

**哲人**：对。所以我们需要第二步：**GRPO**。


## 四、GRPO：让模型自己“赛马”

**哲人**：GRPO是强化学习的一种具体算法。它的核心思想非常巧妙。我先问你：如果你要训练一个模型做数学题，你怎么知道它这步做得好不好？

**青年**：看最后答案对不对。

**哲人**：那如果它答案对，但推理过程是错的呢？

**青年**：那就不能给奖励。

**哲人**：但你怎么知道它的推理过程是错的？你需要一个“过程奖励模型”，但训练那个模型本身就很麻烦。GRPO解决这个问题的方式很简单：**让模型自己生成多个答案，然后比较它们**。

**青年**：比较？

**哲人**：对。比如同一个问题，让模型生成4个答案。有的对，有的错。GRPO不告诉它“这个答案的绝对分数是多少”，而是告诉它“你这个答案比组里的平均水平好多少”。如果它比平均好，就鼓励它；如果比平均差，就抑制它。

**青年**：所以它不需要一个“绝对评分”，只需要一个“相对评分”？

**哲人**：对。这就绕开了“训练奖励模型”这个麻烦步骤。而且，因为比较的是“组内相对表现”，奖励的方差被缩小了，训练更稳定。

**青年**：那它会不会为了拿高奖励，学一些“歪门邪道”？比如把答案写得又长又啰嗦，因为长答案更容易包含正确答案？

**哲人**：这就是为什么要加**KL散度惩罚**。它防止模型为了拿奖励，偏离SFT模型太远。如果它偏离太远，KL散度就变大，奖励就被扣掉。这就像一根橡皮筋，把它拴在SFT模型附近，让它别跑太偏。

**青年**：所以GRPO是“在SFT的基础上，小步探索”？

**哲人**：对。而且它省掉了PPO里的“价值模型”。PPO需要四个模型同时跑，GRPO只需要两个。显存占用更低，工程实现更简单。


## 五、奖励函数：告诉它“什么是好”

**哲人**：现在，我们聊一个最关键、也最容易出问题的地方：**奖励函数**。它是整个强化学习里最“像艺术”的部分。你必须用数值告诉模型：什么是好，什么是坏。

**青年**：如果奖励设计得不好，会怎么样？

**哲人**：它会学会“作弊”。比如你设计一个奖励：“答案里有‘Final Answer: 72’，就给1分。”那它会学会：不管对不对，都写上“Final Answer: 72”。它找到了捷径，但它没有真正解题。这就是所谓的**奖励黑客**。

**青年**：那怎么设计好的奖励？

**哲人**：有几个原则。第一，**奖励要能区分“好”和“更好”**。不能只有“对”和“错”。可以对“接近正确”给部分奖励。第二，**奖励要防止“钻空子”**。比如你奖励“步骤多”，它就会生成一堆没用的步骤。第三，**奖励要稳定**。不能这次给高，那次给低，否则训练不收敛。

**青年**：教程里的“准确率奖励”“长度惩罚”“步骤奖励”，就是三种不同的奖励？

**哲人**：对。准确率奖励只看答案对不对。长度惩罚鼓励它别啰嗦。步骤奖励鼓励它把推理过程写清楚。你可以把它们组合起来，比如：`总奖励 = 准确率 - 长度惩罚 + 步骤奖励`。这样它就会努力答对、答得简洁、答得有逻辑。


## 六、评估：训练完了，怎么知道它变强了？

**哲人**：训练完了，你需要评估它。不能只看一个“准确率”。要看多个维度。

**青年**：比如？

**哲人**：**准确性**：答对的比例。**效率**：平均答案长度、平均推理步数、推理时间。**质量**：格式正确率、推理连贯性、可解释性。而且，你还要做**错误分析**。它不是所有题都错，有的题是计算错，有的是推理错，有的是理解错。你要分类看，才知道下一步该往哪里改进。

**青年**：所以训练不是一次性的，是“训练-评估-分析-改进”的循环？

**哲人**：对。训练不是终点，迭代才是。你训练一次，评估一次，发现“它在计算上老出错”，那你就去加强计算方面的训练。再评估，再分析。它就会一步步变强。


## 尾声：从“构造”到“训练”

**青年**：（长舒一口气）老师，我今晚最大的感受是，我之前一直在“构造”智能体。我给它工具、给它记忆、给它通信。但“构造”是有上限的。我能给的，它就有；我给不了的，它就没有。而“训练”是**让它自己长出能力**。

**哲人**：对。构造是“把东西装进它脑子里”，训练是“让它自己学会怎么用脑子”。

**青年**：而且，训练的本质是**试错**。它可以在一个安全的环境里，失败很多次，然后从失败里学到东西。就像人学走路，摔了很多次，最后学会了。

**哲人**：而这个“安全的环境”，就是你给它设计的**奖励函数**。奖励函数是你和它之间的“对话”——你告诉它“什么是对的”，它用行动回应“我理解了”。

**青年**：那如果我想训练一个智能体，让它学会用工具呢？

**哲人**：那你的奖励函数里，就要包含“工具使用成功”的奖励。让它知道“用工具是对的”。然后它会自己探索：什么时候该用工具？用哪个工具？怎么组合工具？

**青年**：它可能会发现我没想过的工具组合方式。

**哲人**：对。这就是Agentic RL最迷人的地方：**你不是在教它“怎么做”，而是在教它“什么值得做”。** 剩下的，它自己会找到路。

**青年**：（站起来，拿起那张数学卷子）老师，我今晚回去，想试试给我的HelloAgents加一个最基础的SFT流程。不用大模型，就用一个小模型。我想看看，它从“乱答”到“有格式地答”的过程。

**哲人**：去吧。你会看到，当它第一次学会“Step 1: ... Step 2: ...”的时候，它不只是学会了格式，它学会了“**把问题拆开想**”。这是它自己走出的第一步。


**（青年走出咖啡厅，夜风里带着一点数学卷子的油墨味。他知道，自己正在从“建造智能体”走向“教育智能体”。前者是工程，后者是陪伴它成长。而成长，从来都不是一次性的构造，而是一场漫长的、有奖励、有反馈的练习。）**
