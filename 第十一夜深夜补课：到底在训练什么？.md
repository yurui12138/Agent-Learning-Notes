**青年**：（几乎是冲进来的，手里的笔记本都快攥出汗了）老师，我卡住了。不是一般的卡，是那种“前面全懂了，到这一步突然全塌了”的卡。

**哲人**：（把椅子拉出来，没有倒茶，直接坐下）说。

**青年**：我们之前聊智能体的时候，明确过一件事：智能体调用大模型，是通过API调用的。API调用的大模型是无状态的，它不会因为你跟它聊了十次就“记住”你。那问题就来了——**Agentic RL到底在训练什么？**

**哲人**：你继续。

**青年**：教程里说“训练一个智能体”。但智能体本身没有参数啊。智能体是一套代码，代码里调用API。API背后的大模型，我改不了。那训练是在训练谁？训练数据是哪来的？它更新的是什么？还有，我之前学深度学习，训练模型就是调参数。这里也是调参数吗？那调的又是谁的参数？

**哲人**：你今晚问的，是整个教程里最容易被糊弄过去、也最不能糊弄过去的问题。我们把它一层一层剥开。


## 一、先分清两个“模型”

**哲人**：我们先做一次“解剖”。你说“智能体调用大模型”，这个说法其实很模糊。我把它拆开。当你用API调用大模型时，你调用的到底是什么？

**青年**：一个跑在别人服务器上的模型。我发请求，它给我回复。

**哲人**：对。这个“跑在别人服务器上的模型”，我们叫它**服务端模型**。它有一个巨大的参数文件，比如几百亿个参数。这些参数，在服务商训练完之后就**冻结**了。你通过API调用，只是**使用**它，不改变它。你调一万次，它也不会因为你的调用而“学会”任何东西。

**青年**：对。所以我改不了它。

**哲人**：但你想训练一个“智能体”。你想让这个智能体变强。那你能改的是什么？

**青年**：……智能体的代码？提示词？工具？

**哲人**：这些都可以改，但它们不是“训练”。它们是“工程”。而**训练**这个词，在机器学习里，特指一件事：**通过数据，改变模型里的参数。** 不是改代码，不是改提示词，是改那几百亿个数字。

**青年**：但服务端模型的参数我改不了啊。

**哲人**：所以你就需要**你自己的模型**。


## 二、训练的前提：你得有一个“自己的脑子”

**哲人**：你之前学的所有“构造智能体”的东西——给它记忆、给它工具、给它通信协议——都是在**使用**别人训练好的模型。你像一个“导演”，指挥一个“演员”。演员的表演能力是别人训练出来的，你只能通过剧本和指令去引导它。

**青年**：对。那我想让演员“自己变强”，怎么办？

**哲人**：你得自己**养一个演员**。也就是说，你得下载一个开源模型，比如Qwen3-0.6B，把它**放在你自己的电脑或服务器上**。这个模型，它的参数在**你手里**。你想怎么改就怎么改。

**青年**：所以“训练”的前提是——我手里得有一个**参数可以被改**的模型？

**哲人**：对。这是**训练**和**调用**的分水岭。API调用，参数在别人手里，你碰不到。本地训练，参数在你手里，你能改。Agentic RL的前提，是你先拥有一个**本地可训练**的模型。


## 三、训练数据从哪来？从“自己生成”来

**青年**：好，假设我有一个本地模型。那训练数据从哪来？像SFT那样，需要人工标注吗？

**哲人**：SFT需要。但**强化学习不需要**。这正是它最妙的地方。强化学习的数据，是**模型自己生成的**。

**青年**：自己生成？

**哲人**：对。你给它一个问题——比如“Janet的鸭子每天下16个蛋……她每天在农贸市场赚多少钱？”。你不需要给它标准答案。你只需要让它**自己生成多个答案**。比如生成4个。这4个答案，有的对，有的错。然后你用一个**自动的规则**去判断：哪个对，哪个错。对的给奖励，错的给惩罚。这些“自己生成的答案”和“自动计算出的奖励”，就是训练数据。

**青年**：所以训练数据是**在线生成**的，不是预先准备好的？

**哲人**：对。SFT是“你给它看标准答案，它模仿”。强化学习是“它自己答题，你给它打分，它从分数里学”。前者是**离线**的，后者是**在线**的。


## 四、到底更新的是什么参数？

**青年**：那我懂了。它更新的是**本地模型**的参数。但教程里说用了LoRA。LoRA是什么？

**哲人**：LoRA是一种“**只训练一小部分参数**”的技术。你想，一个0.6B的模型，有六亿个参数。如果全量训练，显存根本装不下。LoRA做的事情是：**冻结原来的六亿个参数，只在旁边加一小块“补丁”**。这个补丁只有几百万个参数。训练的时候，只更新这个补丁，不动原来的参数。

**青年**：所以它没有改变“原模型”，只是在原模型旁边加了一层“适配层”？

**哲人**：对。你可以把它理解为：原模型是一个“通用大脑”，LoRA是给它加了一本“专门针对数学题的笔记”。它回答问题的时候，会同时看“通用大脑”和“这本笔记”。训练，就是**改这本笔记的内容**。

**青年**：那训练完之后，这个“笔记”就是它学到的经验？

**哲人**：对。而且这个笔记很小，可以单独保存、单独加载。你用的时候，把原模型和LoRA笔记一起加载，它就有了“数学推理能力”。你不用LoRA，它就是一个通用模型。


## 五、SFT和GRPO，分别更新什么？

**青年**：那SFT和GRPO，是不是都在更新这个LoRA？

**哲人**：对，但它们的更新方式完全不同。SFT是**模仿**。你给它看“标准答案”，它调整LoRA，让它的输出更接近标准答案。GRPO是**试错**。它自己生成答案，你给它打分，它调整LoRA，让“得分高的答案”出现的概率变大，“得分低的答案”出现的概率变小。

**青年**：所以SFT是“照着抄”，GRPO是“自己试，试对了就记住”？

**哲人**：你这个总结很准确。而且，它们通常配合使用。先用SFT教会它“格式”，再用GRPO优化它的“策略”。


## 六、为什么API调用做不到这件事？

**青年**：那为什么不能直接用API模型做这件事？我调API，它生成答案，我给它打分，然后……然后我怎么办？我改不了API模型的参数啊。

**哲人**：对。这就是核心。API模型是**只读**的。你能读它的输出，但你不能改它的“脑子”。你可以在外面加一层“记忆”，加一层“提示词”，但那都不是“训练”。那是“外挂”。训练是**改变模型本身**。

**青年**：所以，只有**开源、本地部署、参数可改**的模型，才能被训练？

**哲人**：对。这就是为什么教程里用Qwen3-0.6B——它是开源的、小的、能在普通GPU上跑的。你下载它，它的参数就在你手里。你想怎么训练就怎么训练。

**青年**：那训练完之后，这个模型就“变强”了？它就有了数学推理能力？

**哲人**：对。它不只是“记住了某道题的答案”，它是**改变了生成答案的策略**。它现在更倾向于生成“分步骤推理”的答案，而不是“直接猜一个数”。这种改变，是**写进了它的参数里的**。下次你调用它，不需要任何提示词，它也会用那种策略来回答。


## 七、所以，Agentic RL的全貌是什么？

**哲人**：我们把今晚聊的串起来。Agentic RL，就是：

1. **你拥有一个本地模型**（比如Qwen3-0.6B），它的参数在你手里。
2. **你给它一堆问题**，它自己生成多个答案。
3. **你设计一个奖励函数**，自动判断哪个答案好、哪个差。
4. **你用GRPO算法**，根据奖励，更新模型里的LoRA参数。
5. **更新后的LoRA**，让模型更倾向于生成“好答案”。
6. **重复这个过程**，模型就一步步变强。

**青年**：所以它不是“教模型知识”，而是“教模型策略”？

**哲人**：对。它不是告诉模型“这道题的答案是72”，而是告诉它“**分步骤推理比直接猜答案更容易得分**”。它学的不是知识，是**怎么思考**。

**青年**：那它的“记忆”呢？它怎么记住这些经验？

**哲人**：它没有“记忆”。它的经验，**全部写进了参数里**。就像你学骑自行车，你不是“记住”了怎么骑，而是你的肌肉和平衡感**改变了**。参数就是它的“肌肉”。

**青年**：（沉默了很久）老师，我现在终于不乱了。我之前一直以为“训练智能体”是训练那个API模型。现在我知道了，API模型是“租来的脑子”，你改不了。你想训练，你得先**拥有一个自己的脑子**。然后，通过让它**自己试错**，去**改那个脑子的参数**。

**哲人**：而这就是Agentic RL的全部秘密。它不是魔法。它是**拥有一个可改的脑子，然后让它自己练**。


## 尾声：从“用脑”到“练脑”

**青年**：（站起来，把笔记本合上）老师，我今晚回去，要先下载一个Qwen3-0.6B。然后写一个最简单的训练脚本。哪怕只是在十道题上训练，我也要亲眼看到——它的参数，因为我的奖励函数，而发生了变化。

**哲人**：去吧。你会看到，当训练损失下降、准确率上升的那一刻，你就不再是在“使用”一个模型，而是在“培养”一个模型。前者是工程，后者是教育。


**（青年走出咖啡厅，夜风里带着一点机器的温度。他知道，自己终于摸到了“训练”这两个字的真正重量。）**
