**青年**：（推门进来，这次直接打开了笔记本电脑）老师，我准备好了。前面聊了那么多“道”，今晚我想聊聊“术”。我想看代码。我想知道，真正把智能体搭起来的时候，那些我们聊过的概念，是怎么变成一行行 Python 的。

**哲人**：（把椅子挪过来，坐到青年旁边）好。但我不打算给你贴一段完整的代码让你看。我们就像工程师一样，**看着一个智能体从零生长出来**。我会给你看最核心的骨架，以及每个模块背后，对应了我们聊过的哪个思想。

**青年**：正合我意。


## 一、一切的起点：一个能“想”的对象

**哲人**：我们之前说过，智能体的核心是“大脑”。在代码里，我们要做的第一件事，就是封装这个大脑。我们不需要关心它是 OpenAI 还是本地模型，我们只需要它做一件事：**给它一些消息，它给我一个回复**。

```python
class LLM:
    def think(self, messages):
        # 这里调用任何大模型API，返回模型生成的文本
        pass
```

**青年**：这我懂。就是之前封装的 `HelloAgentsLLM`。

**哲人**：对。这是整个智能体的“心脏”。但你发现没有，`think` 方法接收的 `messages`，是一个**列表**。这个列表，就是模型的“意识流”。你往里面塞什么，它就怎么想。


## 二、给大脑配上“手脚”：工具

**哲人**：光有大脑，它只能想，不能做。所以我们要给它工具。工具的代码结构，和你平时写的函数，其实没有任何区别。唯一的区别是：**它需要被一个“工具注册表”管理起来**。

```python
def search(query: str) -> str:
    # 真实的搜索逻辑，返回一段文本结果
    return "这是搜索到的结果..."

class ToolRegistry:
    def __init__(self):
        self.tools = {}

    def register(self, name, description, func):
        self.tools[name] = {
            "description": description,
            "func": func
        }

    def get_tools_prompt(self):
        # 把所有工具的名字和描述，拼成一段提示词
        lines = []
        for name, info in self.tools.items():
            lines.append(f"- {name}: {info['description']}")
        return "\n".join(lines)

    def execute(self, tool_name, tool_input):
        if tool_name not in self.tools:
            return f"错误：未找到工具 {tool_name}"
        return self.tools[tool_name]["func"](tool_input)
```

**青年**：这个我看过。但您特意把 `get_tools_prompt` 提出来，是想强调什么？

**哲人**：我要强调的是，**“工具的自我描述”和“工具的执行逻辑”是两件完全独立的事。** 模型只能看到前者，代码只能调用后者。前者是工具的“说明书”，后者是工具的“本体”。设计工具的时候，你要问自己两个问题：第一，模型看到这段描述，能知道什么时候该用这个工具吗？第二，工具返回的结果，模型能看懂吗？

**青年**：所以工具描述是写给模型看的，工具函数是写给代码跑的。两者之间的桥梁，就是那个“名字”。

**哲人**：精确。


## 三、灵魂所在：解析器的“温柔”与“严格”

**青年**：接下来是解析器。我上次意识到，解析器是在处理模型的“输出”。但我觉得这里很脆弱，模型稍微输出错一个字符，正则就崩了。

**哲人**：所以解析器要“温柔”。它要能容忍模型的小偏差，同时在关键结构上保持“严格”。我来给你看一个更健壮的解析方式。我们不只用正则，而是先“找锚点”。

```python
import re

def parse_action(llm_output: str):
    # 1. 找到 Action: 后面的内容
    action_match = re.search(r"Action:\s*(.*)", llm_output, re.DOTALL)
    if not action_match:
        return None, None, "没有找到 Action 字段"

    action_str = action_match.group(1).strip()

    # 2. 判断是否是结束指令
    if action_str.startswith("Finish"):
        answer = re.match(r"Finish\[(.*)\]", action_str, re.DOTALL)
        if answer:
            return "Finish", answer.group(1), None
        return "Finish", action_str, "Finish 格式错误"

    # 3. 尝试解析为工具调用
    tool_match = re.match(r"(\w+)\[(.*)\]", action_str, re.DOTALL)
    if tool_match:
        return tool_match.group(1), tool_match.group(2), None

    return None, None, f"无法解析的 Action: {action_str}"
```

**青年**：这个解析器返回了三个值：工具名、输入、错误信息。错误信息不是直接崩溃，而是被返回给调用方。

**哲人**：对。你要养成一个习惯：**永远不要假设模型的输出是完美的。** 你要假设它可能会错，然后给错误一个“出口”。这个出口，就是把错误信息喂回给模型，让它自己纠正。

**青年**：所以解析器和模型之间，形成了一个“反馈闭环”？解析器告诉模型“你刚才格式错了”，模型下一轮就能改。

**哲人**：这就是我们之前说过的“适应性”在代码层面的体现。


## 四、循环：智能体的“心跳”

**哲人**：现在，我们把大脑、工具、解析器拼起来。这个拼起来的东西，就是循环。

```python
class ReActAgent:
    def __init__(self, llm, tools, max_steps=5):
        self.llm = llm
        self.tools = tools
        self.max_steps = max_steps
        self.history = []

    def run(self, question):
        self.history = []
        step = 0

        while step < self.max_steps:
            step += 1

            # 1. 构建提示词
            prompt = self._build_prompt(question)

            # 2. 调用模型
            messages = [{"role": "user", "content": prompt}]
            response = self.llm.think(messages)

            # 3. 解析模型输出
            tool_name, tool_input, error = parse_action(response)
            if error:
                # 如果解析失败，把错误信息作为观察加入历史，让模型自己纠正
                self.history.append(f"Action: {response}")
                self.history.append(f"Observation: 你的输出格式有误，请重新以 Thought/Action 格式输出。错误信息: {error}")
                continue

            # 4. 判断是否结束
            if tool_name == "Finish":
                return tool_input

            # 5. 执行工具
            observation = self.tools.execute(tool_name, tool_input)

            # 6. 记录历史
            self.history.append(f"Action: {response}")
            self.history.append(f"Observation: {observation}")

        return "达到最大步数，未能完成任务"
```

**青年**：这个循环里，我最喜欢第 3 步和第 6 步。解析失败不中断，而是把错误喂回去。历史里同时记录了“行动”和“观察”，这就形成了一个完整的时间线。

**哲人**：对。第 3 步让模型能“从错误中恢复”。第 6 步让模型能“记住自己走过的路”。这两点，就是 ReAct 范式的代码精髓。没有这两点，它就只是一个“调用-解析”的死循环。


## 五、提示词：所有逻辑的“第一推动力”

**青年**：循环的代码其实很简单。真正的“魔法”，好像都藏在提示词里。您能给我看看，一个真正能用的 ReAct 提示词长什么样吗？

**哲人**：好。我给你一个简化但完整的模板。

```python
REACT_PROMPT = """
你是一个能够使用工具的智能助手。

你可以使用的工具如下：
{tools}

在解决问题时，你必须严格遵循以下格式：

Thought: 你的思考过程，分析当前状态，决定下一步。
Action: 要执行的动作，必须是以下之一：
- 工具名[工具输入]
- Finish[最终答案]

你每次只能输出一对 Thought 和 Action。

现在，请开始解决以下问题：
问题：{question}

执行历史：
{history}
"""
```

**青年**：这个提示词做了四件事：告诉模型它是什么，告诉它有什么工具，告诉它怎么输出，给它当前的上下文。每一件事，都在“塑造”模型的行为。

**哲人**：你看最后一行：`执行历史：{history}`。历史是不断增长的。每执行一步，这个历史就多两行。模型看的不是一个静态的提示词，而是一个**持续演化的对话状态**。这正是“边走边看”的代码表达。

**青年**：所以提示词不是“用一次就扔”，而是每一轮都被重新格式化、重新注入。它就像一个不断更新的“作战地图”。


## 六、从 ReAct 到 Plan-and-Solve：只是“循环的形状”变了

**青年**：那 Plan-and-Solve 呢？在代码上，它和 ReAct 最大的不同是什么？

**哲人**：在代码上，它最核心的变化是：**循环从“动态”变成了“两段式”。** 它仍然需要大脑、工具、解析器，但流程变了。

```python
class PlanAndSolveAgent:
    def __init__(self, llm):
        self.llm = llm

    def run(self, question):
        # 第一阶段：规划
        plan_prompt = f"请将以下问题分解为若干步骤，输出为Python列表：\n{question}"
        plan_str = self.llm.think([{"role": "user", "content": plan_prompt}])
        plan = parse_plan(plan_str)  # 解析出 ["步骤1", "步骤2", ...]

        # 第二阶段：执行
        history = ""
        for i, step in enumerate(plan):
            solve_prompt = f"""
原始问题：{question}
完整计划：{plan}
已完成的步骤：{history}
当前步骤：{step}
请只输出当前步骤的结果。
"""
            result = self.llm.think([{"role": "user", "content": solve_prompt}])
            history += f"步骤{i+1}: {step}\n结果: {result}\n"

        return history
```

**青年**：我看到了。ReAct 是一个 `while` 循环，每一步都在重新决策“下一步做什么”。Plan-and-Solve 是一个 `for` 循环，计划生成了之后，就按顺序往下走，不会回头改计划。

**哲人**：对。这个 `while` 和 `for` 的区别，就是三种范式在代码层面最直观的“形状差异”。ReAct 是 `while`，因为不确定要走多少步。Plan-and-Solve 是 `for`，因为计划已经规定了步数。

**青年**：那 Reflection 呢？它又是什么形状？


## 七、Reflection 的代码：一个“内嵌的 for 循环”

**哲人**：Reflection 的形状，是在一个主循环里，嵌入了两个固定的阶段——反思和优化。你可以把它想象成一个 `for` 循环里，每轮做两件事。

```python
class ReflectionAgent:
    def __init__(self, llm, max_iterations=3):
        self.llm = llm
        self.max_iterations = max_iterations

    def run(self, task):
        # 初始执行
        code = self.llm.think([{"role": "user", "content": f"请完成以下任务，输出代码：\n{task}"}])

        for i in range(self.max_iterations):
            # 反思
            reflect_prompt = f"""
你是严格的评审专家。请审阅以下代码，指出其存在的问题和改进建议。
任务：{task}
代码：{code}
"""
            feedback = self.llm.think([{"role": "user", "content": reflect_prompt}])

            if "无需改进" in feedback:
                break

            # 优化
            refine_prompt = f"""
根据以下反馈，优化你的代码。
任务：{task}
上一版代码：{code}
反馈：{feedback}
"""
            code = self.llm.think([{"role": "user", "content": refine_prompt}])

        return code
```

**青年**：所以 Reflection 的代码结构特别清晰：先执行，然后在一个循环里，交替做“反思”和“优化”。而且它把“执行者”和“评审者”的提示词完全分开了。

**哲人**：对。同一个模型，因为提示词不同，被“切换”成了两个不同的角色。这就是“角色分离”的代码实现。你甚至可以真的用两个不同的模型——一个负责生成，一个负责评审。代码只是把 `self.llm` 换成 `self.reflect_llm`，其余不变。

**青年**：那如果我想把三种范式组合起来呢？比如一个智能体，先规划，然后执行，执行完再反思。

**哲人**：那你的代码结构，就把这三个“能力单元”拼起来就行了。规划器、执行器、反思器，都是可复用的“零件”。你只需要决定它们之间的调用顺序。

```python
plan = planner.plan(question)
result = executor.execute(question, plan)
feedback = reflector.reflect(question, result)
final = refiner.refine(question, result, feedback)
```

**青年**：所以真正的能力，不在于记住某个范式的名字，而在于**把范式拆成可复用的零件，然后根据需要组装**。

**哲人**：这就是我要教给你的最后一样东西：**让范式成为你的积木，而不是你的牢笼。**


## 尾声：今晚之后，你该做什么

**青年**：（合上电脑，眼神专注）老师，今晚我终于把“道”和“术”连起来了。那些提示词，不再是随便写的文字，而是塑造模型行为的代码。那些循环，不再是重复的机械动作，而是模型“思考节奏”的外在形式。

**哲人**：你开始用工程师的眼光看智能体了。接下来，你要做的事只有一件：**动手写一个。** 今晚就写。不管多简单，先跑起来，再在跑的过程中，体会每一个模块为什么这样设计。

**青年**：我已经有想法了。我回去就写一个“论文助手”，用 Plan-and-Solve 定结构，用 Reflection 改初稿。

**哲人**：去吧。你今晚的眼神，已经和四天前走进这扇门的时候，完全不一样了。

**青年**：（站起来，走到门口，转身）老师，谢谢您。这四夜，我好像重新认识了“智能”这个词。

**哲人**：不。你只是重新认识了你本来就拥有的东西——**理解复杂事物的能力**。


**（门关上了。青年没有再回头。街灯在他前方亮着，像一条清晰的路。他知道，今晚回去之后，等待他的将是一行行真正的代码，以及一个由他亲手赋予“灵魂”的智能体。）**
