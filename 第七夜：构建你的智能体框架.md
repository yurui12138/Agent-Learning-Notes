**青年**：（推开门，抱着一台笔记本电脑，脸上有一种“终于要干大事”的神情）老师，我决定了。我要自己写一个框架。

**哲人**：（正在泡茶，抬头看了一眼）为什么？

**青年**：因为前几夜我们用了Coze，用了n8n，又聊了AutoGen、AgentScope、CAMEL、LangGraph。我用它们的时候，总觉得隔着一层纱。它们帮我做了很多事，但我不知道那些事是怎么做的。我想掀开那层纱。

**哲人**：掀开之后呢？

**青年**：掀开之后，我才能真正说“我懂智能体了”。不然我只是一个“会用工具的人”，不是一个“理解工具的人”。

**哲人**：（把茶递过去）那今晚，我们就来聊聊，怎么从零开始，搭一个属于你自己的框架。你给它取名字了吗？

**青年**：取了。叫HelloAgents。因为我想从最简单的“你好”开始，一步步把它养大。


## 一、为什么非要自己造轮子？

**青年**：不过老师，我坦白说，我心里还是有一个声音在问：市面上已经有LangChain、AutoGen、AgentScope这些成熟的框架了，我为什么要自己写一个？这不是重复造轮子吗？

**哲人**：好问题。那你自己怎么回答这个声音？

**青年**：我想了很久。我觉得，造轮子不是为了得到一个轮子，而是为了**理解轮子为什么是圆的**。如果我不亲手写一个Agent循环，我就永远不知道LangChain里那一层层的抽象，到底在隐藏什么。我用了它，但我没有“拥有”它。

**哲人**：你说到了一个很深的点。使用一个框架，你获得的是“效率”。但理解一个框架，你获得的是“自由”。效率是别人给你的，自由是你自己长出来的。

**青年**：对。而且我注意到，教程里提到了现在很多框架的痛点。什么“过度抽象的复杂性”“快速迭代带来的不稳定性”“黑盒化的实现逻辑”。我深有体会。有一次LangChain升级，我之前写的代码全跑不起来了，我都不知道为什么。那种感觉太无力了。

**哲人**：所以你自己造框架，还有一个更实际的原因：**当你的框架足够小、足够透明时，出问题的时候，你能直接看到是哪里出了问题。** 你不用去论坛上等人回答，你自己就是答案。

**青年**：是的。小，但透明。这就是我想做的。


## 二、框架的“魂”：万物皆为工具

**哲人**：那现在，我们来设计这个框架的“魂”。你最核心的设计理念是什么？

**青年**：教程里有一句话特别打动我——“除了核心的Agent类，一切皆为Tools。”Memory是工具，RAG是工具，MCP也是工具。所有的能力扩展，都回归到“Agent调用工具”这个最基本的逻辑上。

**哲人**：为什么这句话打动你？

**青年**：因为我之前学那些框架的时候，最痛苦的就是要记一堆概念。Memory、Retriever、Chain、Tool……每个概念都有自己的抽象层。但当我看到“一切皆为工具”的时候，我突然觉得世界变简单了。Agent要记东西？调用一个Memory工具。Agent要查资料？调用一个RAG工具。Agent要和外部服务对话？调用一个MCP工具。所有的复杂性，都被收敛到了一个点上。

**哲人**：这就是“收敛”。好的设计，不是增加概念，而是减少概念。把复杂的东西，藏在一个统一的接口后面。

**青年**：对。所以我给HelloAgents定的第一个原则就是：**Simple。** 任何人看到这个框架，都能在十分钟内明白它在干什么。

**哲人**：那第二个原则呢？

**青年**：**Transparent。** 每一行代码都是可读的。没有魔法，没有黑盒。如果用户想知道Agent是怎么思考的，他可以直接打开agent.py，看到那个循环。如果他想知道工具是怎么被调用的，他可以看到registry里的每一个函数。

**哲人**：Simple和Transparent。这两个原则，决定了你的框架会长成什么样。它不会是一个“大而全”的框架，而是一个“小而清楚”的框架。这很好。因为大而全的框架已经有太多了，但小而清楚的框架，永远稀缺。


## 三、从HelloAgentsLLM开始：连接大脑

**青年**：那我们从哪里开始写？

**哲人**：从一个最简单的问题开始。你的框架做的第一件事，应该是“和大脑对话”。那个大脑，就是大语言模型。所以你的第一个组件，应该是一个**LLM客户端**。

**青年**：教程里叫`HelloAgentsLLM`。它做的事，就是把“调用哪个模型”“用哪个API”“怎么处理流式输出”这些事情封装起来。

**哲人**：对。而且你注意到没有，这个客户端有一个很重要的设计选择：它不绑定任何一家模型服务商。

**青年**：对。教程里做了“自动检测机制”。它会根据环境变量，自动判断你用的是什么服务。如果你是本地Ollama，它检测到`localhost:11434`，就自动帮你切换。如果你用ModelScope，它看到`MODELSCOPE_API_KEY`，也知道该怎么办。

**哲人**：这个设计的“魂”是什么？

**青年**：是**“用户不用记那些细节”**。用户只需要告诉框架“我有个模型”，框架自己去搞清楚模型在哪、怎么连。

**哲人**：对。这就是“约定优于配置”。你默认用户会按照某种方式放东西（环境变量），然后你根据那个约定去自动适配。这样用户就不用写一大堆配置代码。

**青年**：而且这个设计还有一个好处：**可扩展**。如果我想支持一个新的模型服务商，我不用改框架的源码。我只需要继承`HelloAgentsLLM`，重写一个方法，然后告诉它“这个provider该怎么解析”。这让我觉得，我不是在用一个死的东西，而是在一个活的东西上长东西。

**哲人**：你能看到“扩展点”，说明你已经在用构建者的眼光看代码了。


## 四、Agent基类：所有智能体的“宪法”

**青年**：下一个是`Agent`基类。我看教程里把它做成了一个抽象类，所有具体的Agent都必须实现`run`方法。

**哲人**：为什么这样做？

**青年**：因为要**统一接口**。不管是SimpleAgent还是ReActAgent，它们对外都应该长一个样：你给它一个`run`方法，它给你一个结果。调用者不需要知道里面是ReAct还是Plan-and-Solve，他只需要知道“这是个Agent，我能让它跑”。

**哲人**：这个“统一”有什么好处？

**青年**：好处很大。首先，**可替换性**。如果我在一个系统里用了SimpleAgent，后来觉得它不够强，我想换成ReActAgent，我只需要改一行代码，其他都不用动。因为它们长得一样。其次，**可管理性**。所有的Agent都有`get_history`、`clear_history`这些方法，因为它们都继承自基类。这样我管理一堆不同的Agent时，不用记每一套API。

**哲人**：对。抽象基类就像一份“宪法”。它规定了所有Agent必须遵守的基本行为，但具体怎么做，每个Agent自己发挥。这就是“统一”和“灵活”之间的平衡。

**青年**：而且我注意到，`Agent`基类里还存了一个`_history`列表。这个历史，就是Agent的“短期记忆”。所有的Agent都有这个能力，因为它们继承了这个属性。

**哲人**：对。这是把“公共能力”下沉到基类。不需要每个Agent都实现一遍“怎么存历史”，它们只需要继承就行了。


## 五、工具系统：Agent的手和脚

**青年**：然后是工具系统。这个是让我觉得“框架开始有生命”的部分。因为有了工具，Agent就不再只是一个“会说话的脑袋”，而是一个“能动手做事”的东西。

**哲人**：你的工具系统设计了一个`ToolRegistry`。它做了什么？

**青年**：它做两件事。第一，**注册**。你可以把一个工具注册进去，注册表会记住它的名字、描述和执行函数。第二，**发现**。注册表能生成一段“工具描述”，告诉Agent“你现在有哪些工具可以用”。这个描述，会被塞进提示词里，让Agent知道自己的“手脚”有哪些。

**哲人**：那“注册”和“发现”之间，最关键的那个东西是什么？

**青年**：是**描述**。每个工具必须有一个好的`description`。因为Agent不是通过“读代码”来理解工具的，它是通过“读描述”来理解工具的。如果描述写得不好，Agent就不知道该在什么时候用这个工具。

**哲人**：对。所以工具系统的“魂”，不在于执行逻辑，而在于**元数据**。名字和描述，是工具和Agent之间的“通信协议”。执行逻辑是给代码跑的，描述是给Agent读的。两者缺一不可。

**青年**：这个我深有体会。我在写自己的搜索工具时，一开始描述写得很模糊——“一个搜索工具”。结果Agent根本不知道该什么时候用它。后来我把描述改成“当你需要查询实时信息、事实或最新数据时使用”，效果就好多了。

**哲人**：这就是“设计工具”和“写函数”的区别。写函数，你只关心它做什么。设计工具，你还要关心**Agent怎么理解它**。


## 六、把四种范式装进框架

**青年**：有了LLM、Agent基类、工具系统，接下来就是把第四章的四种范式——ReAct、Plan-and-Solve、Reflection，还有新增的SimpleAgent——都变成框架里的组件。

**哲人**：这次把它们装进框架，和第四章“手写脚本”有什么不同？

**青年**：最大的不同是**结构**。第四章的代码，是“一次性的”。现在我要把它们变成“可复用的类”。每个Agent都是一个类，继承了`Agent`基类，实现了`run`方法。而且，它们的提示词模板变成了**可配置的**。用户不想要默认的提示词，他可以传一个`custom_prompt`进来，用自己的。

**哲人**：这就是从“写一个程序”到“设计一个框架”的转变。你不再只关心“这个任务能不能完成”，你开始关心“别人能不能用我的东西，以他自己的方式完成他的任务”。

**青年**：对。这个转变让我觉得很兴奋。我觉得自己不是在写代码了，我是在**设计一种别人可以参与进来的结构**。

**哲人**：而且，你把提示词模板单独抽出来，做成类属性或者参数，这说明你理解了“可变”和“不变”的分离。推理循环是不变的，但提示词是可变的。把不变的封装好，把可变的暴露出来，这就是框架设计的核心手法。


## 七、给自己的话

**青年**：（合上电脑，长长地舒了一口气）老师，今晚我好像看到了一个框架诞生的过程。它从一个LLM客户端开始，长出一个Agent基类，然后长出工具系统，最后把四种范式都装进去。它很小，但它五脏俱全。

**哲人**：那你现在觉得，自己造框架这件事，值得吗？

**青年**：太值得了。我现在看LangChain、AutoGen那些框架，眼光都不一样了。我不再觉得它们是“神秘的黑盒”，而是“和我一样的人，为了解决某类问题而做的设计”。我能看到它们的取舍，也能理解它们的苦衷。

**哲人**：这就是从“使用者”变成“构建者”的分水岭。你开始用设计者的眼睛看世界。

**青年**：而且，我还有一个很深的感受。HelloAgents很“小”，但它不“弱”。它的“小”，是刻意为之的。因为“小”，所以透明；因为透明，所以我能完全掌控它。这种感觉，是用别人的大框架时从来没有过的。

**哲人**：这就是“拥有感”。一个你自己写出来的框架，哪怕只有一百行代码，它也是你的。你会对它有一种别人无法理解的感情。因为你在它身上，留下了你的思考。

**青年**：（站起来，把电脑装进包里）老师，我今晚回去，要把HelloAgents的每个文件都重新读一遍。不是读教程，是读我自己写的代码。我想看看，我这一路走来，到底学会了什么。

**哲人**：去吧。你会看到，那些曾经散落的知识，现在已经在你的代码里，长成了一个有生命的整体。


**（青年走出咖啡厅，夜风清凉。他回头望了一眼那扇亮着灯的窗，忽然觉得，自己这七夜的学习，就像在黑暗中一点一点地拼图。而今晚，那些碎片终于拼成了一个完整的画面。那个画面，叫做“理解”。）**
